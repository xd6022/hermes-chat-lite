<script setup lang="ts">
/**
 * 轮首头像 —— 每一轮开头的那个 `-_-`。
 *
 * 2026-09-16 用户定：**这里只留头像，不挂状态灯**。理由（实测用出来的结论）：
 *  · 状态已经由**输入框上方那条四态状态行**统一说了，而且它位置固定、永远看得见 ——
 *    轮首再挂一个灯，就是同一件事的两个面孔（这个标记最初的价值是"长轮次滚出视口时还能看到"，
 *    那条需求已被位置固定的状态行覆盖）；
 *  · 而轮首的"历史状态"清一色是 🟢，满屏绿点等于没有信息量；真出过问题的轮，
 *    轮尾本来就有红字块 /「已中断」标记。
 * ⇒ 这一行只回答"**这是谁说的**"，状态一律去上面那条看。
 *
 * 顺便，这也消掉了原来那条硬约束（"跑着时就得把那一行画出来、跑完只换颜色"）——
 * 头像本来就是静态的，不存在高度跳变。
 */
import { avatar } from '../lib/appearance'
</script>

<template>
  <div
    data-testid="turn-avatar"
    class="select-none text-xs leading-5 text-gray-400 dark:text-gray-500"
  >
    {{ avatar }}
  </div>
</template>
