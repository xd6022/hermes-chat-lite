# 会话数据事故记录：api_1789129246_0c6a6ef6

**事故时间**：2026-09-11 20:33:29（北京时间）
**操作**：Hermes 清理测试会话时，用 `source=api_server` 做条件批量删除 → 把用户的真实会话一起删了
**后果**：会话行与消息行被硬删除（`db.delete_session`），**不可恢复**
**恢复尝试（均失败）**：state.db / WAL 页扫描 → 被删页已被后续写入复用，提取到的都是活跃会话的字节；
无 transcript 副本；MySQL 归档 cron（每日 21:00）尚未跑到这条（20:20 创建、20:33 删除）。

## 能抢救回来的部分（来自 `logs/agent.log`，逐字）

**会话标题（Hermes 自动生成）**：`Introduce yourself as Qwen`
**模型**：qwen3.8-flash @ xyy（`token.1597133.xyz`）· 来源：api_server（Web 前端）· 设备：Android Edge

### 第 1 轮 · 20:20:46
- 用户：`你是谁`
- 助手：回复正文**未保存**（长度 183 字符）；无工具调用
- 耗时 38.4s · 输入 27,307 · 输出 140 · 缓存命中 0%（新会话首轮全量 prefill）

### 第 2 轮 · 20:21:54
- 用户：`能识别出来我是从哪里发送过来的消息么？`
- 助手：回复正文**未保存**（长度 378 字符）；调用了工具：`session_search` → `terminal` ×2
- 耗时 31.5s（4 次模型调用：7.1 / 8.1 / 2.8 / 13.2s）· 缓存命中 91–98%

### 第 3 轮 · 20:23:20
- 用户：`good ，很棒，哈哈`
- 助手：回复正文**未保存**（长度 110 字符）
- 耗时 6.6s · 输入 30,559 · 输出 94 · 缓存命中 98%

（会话统计：10 条消息、5 次模型调用、输入 32,613 / 输出 1,438 tokens）

## 教训（已固化为规则）

1. **禁止按 `source` 批量删除会话**。清理测试数据必须用**显式 id 白名单**，并在删除前打印将要删除的 id + 标题 + 消息数供确认。
2. 本项目自己的测试脚本创建的会话，一律当场记录 id，事后按 id 删。
3. `DELETE /api/sessions/{id}` 是**硬删除**（会话 + 消息一起没），Hermes 没有回收站，MySQL 归档只在每日 21:00 跑一次 —— 21:00 之前删除的内容没有第二份副本。
